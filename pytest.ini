[pytest]
minversion = 7.0
addopts = -q -ra -m "not integration" --strict-markers
testpaths = tests
pythonpath = src

markers =
    integration: marks tests requiring external services or credentials (deselect with '-m "not integration"')

log_cli = true
log_cli_level = INFO

filterwarnings =
    ignore::DeprecationWarning
